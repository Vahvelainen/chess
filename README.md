# chess-player

React + TypeScript project bootstrapped with Vite. The page is intentionally blank; start building in `src/App.tsx`.

## Scripts

- `yarn dev` – start dev server
- `yarn build` – production build
- `yarn preview` – preview built assets
- `yarn typecheck` – run TypeScript
